// import FormulariosApp from '@/features/formularios-platform/App';

/**
 * FormularioPage - Mobile
 * Renderiza a plataforma completa de formulários + WhatsApp
 */
const FormularioPage = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <div className="text-center">
        <h1 className="text-2xl font-bold">Módulo de Formulários Indisponível</h1>
        <p className="text-muted-foreground">O recurso solicitado não foi encontrado nesta instalação mobile.</p>
      </div>
    </div>
  );
};

export default FormularioPage;
